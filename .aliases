alias g='git'
alias gt='git'
alias gi='git'

alias ls='ls -G'
alias ll='ls -lhG'
alias la='ls -AhG'
alias lal='ls -AlhG'

alias mkdir='mkdir -p'

alias be='bundle exec'

alias syn='/Applications/Synergy.app/Contents/MacOS/synergys'
alias python='python3'
alias serve='python -m SimpleHTTPServer'

alias vim='mvim -v'
alias vi='vim'
alias emacs='/usr/local/Cellar/emacs/24.2/bin/emacs'

alias tl='tmux ls'
alias ta='tmux attach -t $1'
alias tn='tmux new -s $1'

# iTerm tab/window naming
function name() {
  echo -e "\033];$1\007";
}

# TODO: Turn this into a function that takes the port as an arg
alias tcp4567="sudo tcpdump -s 0 -A -i lo0 'tcp port 4567 and (((ip[2:2] - ((ip[0]&0xf)<<2)) - ((tcp[12]&0xf0)>>2)) != 0)'"

# Just for fun...
alias :x='echo You\ lose!'
alias :q='echo You\ lose!'
alias fuckit='a=1;x=1;y=1;xd=1;yd=1;while true;do if [[ $x == $LINES || $x == 0 ]]; then xd=$(( $xd *-1 )) ; fi ; if [[ $y == $COLUMNS || $y == 0 ]]; then yd=$(( $yd * -1 )) ; fi ; x=$(( $x + $xd )); y=$(( $y + $yd )); printf "\33[%s;%sH\33[48;5;%sm \33[0m" $x $y $(($a%199+16)) ;a=$(( $a + 1 )) ; sleep 0.001 ;done'
alias fuckoff='j=0;a=1;x=1;y=1;xd=1;yd=1;while true;do for i in {1..2000} ; do if [[ $x == $LINES || $x == 0 ]]; then xd=$(( $xd *-1 )) ; fi ; if [[ $y == $COLUMNS || $y == 0 ]]; then yd=$(( $yd * -1 )) ; fi ; x=$(( $x + $xd )); y=$(( $y + $yd )); printf "\33[%s;%sH\33[48;5;%sm . \33[0m" $x $y $(( $a % 8 + 16 + $j % 223 )) ;a=$(( $a + 1 )) ; done ; x=$(( x%$COLUMNS + 1 )) ; j=$(( $j + 8 )) ;done'

