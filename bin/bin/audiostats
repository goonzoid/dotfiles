#!/usr/bin/env bash

shopt -s nocaseglob

files="*.wav"

echo "  #  sample rate"
for file in $files; do
  soxi -r "$file"
done | sort | uniq -c

echo ""

echo "  #  bit depth"
for file in $files; do
  soxi -b "$file"
done | sort | uniq -c
